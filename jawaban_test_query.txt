Nama          : Lisa Amri Mubarokah
Alamat Email  : lisaamrimubarokah22@gmail.com
Waktu Mulai   : Rabu, 13 Juli 2022 pukul 10:00 WIB
Waktu Selesai : Rabu, 13 Juli 2022 pukul 10:40 WIB

1. buatlah query untuk menampilkan peserta dengan gaji tertinggi
   SELECT nama FROM kawahedukasi ORDER BY gaji DESC;

2. buatlah query untuk menampilkan peserta BACKEND dengan nilai tertinggi dan gaji tertinggi
   - query untuk menampilkan peserta BACKEND dengan nilai tertinggi
   SELECT nama FROM kawahedukasi WHERE kelas='BACKEND' ORDER BY nilai DESC;

   - query untuk menampilkan peserta BACKEND dengan gaji tertinggi
   SELECT nama FROM kawahedukasi WHERE kelas='BACKEND' ORDER BY gaji DESC;

3. buatlah query untuk melihat total peserta yang berada dalam kelas frontend
   SELECT COUNT(*) FROM kawahedukasi WHERE kelas='FRONTEND';

4. buatlah query untuk menampilkan jumlah kehadiran tertinggi dan nilai tertinggi dalam kelas frontend
   SELECT MAX(jumlah_kehadiran) as 'jumlah kehadiran tertinggi', MAX(nilai) as 'nilai tertinggi' FROM kawahedukasi WHERE kelas='FRONTEND';

   - query untuk menampilkan jumlah kehadiran tertinggi dalam kelas frontend:
   SELECT jumlah_kehadiran FROM kawahedukasi WHERE kelas='FRONTEND' ORDER BY jumlah_kehadiran DESC;

   - query untuk menampilkan nilai tertinggi dalam kelas frontend:
   SELECT nilai FROM kawahedukasi WHERE kelas='FRONTEND' ORDER BY nilai DESC;
   